services:
  - type: web
    name: vendorsentry-risk
    runtime: python
    buildCommand: >-
      pip install -r requirements.txt &&
      python manage.py collectstatic --noinput &&
      python manage.py migrate
    startCommand: gunicorn config.wsgi:application
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vendorsentry-db
          property: connectionString
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_ALLOWED_HOSTS
        value: vendorsentry-risk.onrender.com
      - key: DJANGO_CSRF_TRUSTED_ORIGINS
        value: https://vendorsentry-risk.onrender.com

databases:
  - name: vendorsentry-db
    plan: free
