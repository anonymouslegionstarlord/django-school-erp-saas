services:
  - type: web
    name: routepilot
    runtime: python
    plan: free
    rootDir: daily-saas-products/2026-09-01-routepilot-fleet-dispatch
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: python manage.py migrate && gunicorn config.wsgi:application
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.5
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_ALLOWED_HOSTS
        sync: false
      - key: DJANGO_CSRF_TRUSTED_ORIGINS
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: routepilot-db
          property: connectionString

databases:
  - name: routepilot-db
    plan: free
    databaseName: routepilot
    user: routepilot
