[tool.ruff]
target-version = "py312"
line-length = 140
exclude = ["support/migrations"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.coverage.run]
source = ["support"]
omit = ["support/migrations/*", "support/management/*"]

[tool.coverage.report]
show_missing = true
fail_under = 80
