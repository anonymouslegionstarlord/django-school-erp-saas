services:
  - type: web
    name: talentnest-ats
    runtime: python
    buildCommand: >-
      pip install -r requirements.txt &&
      python manage.py collectstatic --noinput &&
      python manage.py migrate
    startCommand: gunicorn config.wsgi:application
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: talentnest-db
          property: connectionString
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_ALLOWED_HOSTS
        value: talentnest-ats.onrender.com
      - key: DJANGO_CSRF_TRUSTED_ORIGINS
        value: https://talentnest-ats.onrender.com

databases:
  - name: talentnest-db
    plan: free
