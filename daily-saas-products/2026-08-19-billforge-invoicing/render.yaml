services:
  - type: web
    name: billforge-invoicing
    runtime: docker
    dockerContext: .
    healthCheckPath: /
    envVars:
      - {key: DATABASE_URL, fromDatabase: {name: billforge-db, property: connectionString}}
      - {key: DJANGO_SECRET_KEY, generateValue: true}
      - {key: DJANGO_DEBUG, value: "False"}
      - {key: DJANGO_ALLOWED_HOSTS, value: .onrender.com}
databases:
  - {name: billforge-db, plan: free}
